// this is here because otherwise deno automatically tries to install all our
// stupid deps from package.json even though the script does not want them
{
  "fmt": {
    "options": {
      "semiColons": false,
      "singleQuote": true
    }
  }
}
