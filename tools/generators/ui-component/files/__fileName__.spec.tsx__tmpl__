import React from 'react';
import { render } from '<%= testingUtilsPath %>';

import <%= className %> from './<%= fileName %>';

describe('<%= className %>', () => {
  it('should render successfully', () => {
    const { baseElement } = render(< <%= className %> />);
    expect(baseElement).toBeTruthy();
  });
});