<!-- Avatar.stories.mdx -->

import { ArgsTable, Canvas, Story, Meta } from '@storybook/addon-docs/blocks'
import { Avatar, AvatarProps, avatarSizes } from './Avatar'

<Meta
  title="Components/Avatar"
  component={Avatar}
  argTypes={{
    isPerson: {
      control: {
        type: 'boolean',
      },
    },
    name: {
      control: {
        type: 'text',
      },
    },
    src: {
      control: {
        type: 'text',
      },
    },
    size: {
      control: {
        type: 'select',
        options: Object.keys(avatarSizes),
      },
    },
  }}
/>

# Avatar

Avatars are used to represent a unique entity, be it a person, a team, an org, or a project.

export const Template = (args) => <Avatar {...args} />

export const PersonArgs = {
  name: 'Cameron Howe',
}

export const OrgArgs = {
  name: 'Colossal Cave Adventure',
}

<Canvas>
  <Story name="Default" args={{ ...OrgArgs, size: 'base' }}>
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable of={Avatar} story="^" />

## Design

Avatars representing **persons** use a circular shape and avatars representing **a group or a project** use a square shape. They come in a variety of sizes. The size of an avatar varies depending upon the element it resides in as well as the size of the viewport.

It is preferable that an avatar display an image. Images are personalized and make good use of the ability of the human brain to recognize faces. Project and org avatars contain a fallback in the case an image is not provided. This fallback will display the first letter(s) of the project or org name. Personal avatars have a Gravatar fallback which contains either a configured image or a randomly generated image.

## Stories

### Group or project fallback

<Canvas>
  <Story name="xs" args={{ ...OrgArgs, size: 'xs' }}>
    {Template.bind({})}
  </Story>
  <Story name="sm" args={{ ...OrgArgs, size: 'sm' }}>
    {Template.bind({})}
  </Story>
  <Story name="base" args={{ ...OrgArgs, size: 'base' }}>
    {Template.bind({})}
  </Story>
  <Story name="lg" args={{ ...OrgArgs, size: 'lg' }}>
    {Template.bind({})}
  </Story>
  <Story name="xl" args={{ ...OrgArgs, size: 'xl' }}>
    {Template.bind({})}
  </Story>
  <Story name="2xl" args={{ ...OrgArgs, size: '2xl' }}>
    {Template.bind({})}
  </Story>
</Canvas>

### Group or project with an image

<Canvas>
  <Story
    name="Group - xs"
    args={{
      ...OrgArgs,
      src: 'https://placekitten.com/500/500',
      size: 'xs',
    }}
  >
    {Template.bind({})}
  </Story>
  <Story
    name="Group - sm"
    args={{
      ...OrgArgs,
      src: 'https://placekitten.com/500/500',
      size: 'sm',
    }}
  >
    {Template.bind({})}
  </Story>
  <Story
    name="Group - base"
    args={{
      ...OrgArgs,
      src: 'https://placekitten.com/500/500',
      size: 'base',
    }}
  >
    {Template.bind({})}
  </Story>
  <Story
    name="Group - lg"
    args={{
      ...OrgArgs,
      src: 'https://placekitten.com/500/500',
      size: 'lg',
    }}
  >
    {Template.bind({})}
  </Story>
  <Story
    name="Group - xl"
    args={{
      ...OrgArgs,
      src: 'https://placekitten.com/500/500',
      size: 'xl',
    }}
  >
    {Template.bind({})}
  </Story>
  <Story
    name="Group - 2xl"
    args={{
      ...OrgArgs,
      src: 'https://placekitten.com/500/500',
      size: '2xl',
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

### Person fallback

<Canvas>
  <Story
    name="Person - xs"
    args={{
      ...PersonArgs,
      isPerson: true,
      size: 'xs',
    }}
  >
    {Template.bind({})}
  </Story>
  <Story
    name="Person - sm"
    args={{
      ...PersonArgs,
      isPerson: true,
      size: 'sm',
    }}
  >
    {Template.bind({})}
  </Story>
  <Story
    name="Person - base"
    args={{
      ...PersonArgs,
      isPerson: true,
      size: 'base',
    }}
  >
    {Template.bind({})}
  </Story>
  <Story
    name="Person - lg"
    args={{
      ...PersonArgs,
      isPerson: true,
      size: 'lg',
    }}
  >
    {Template.bind({})}
  </Story>
  <Story
    name="Person - xl"
    args={{
      ...PersonArgs,
      isPerson: true,
      size: 'xl',
    }}
  >
    {Template.bind({})}
  </Story>
  <Story
    name="Person - 2xl"
    args={{
      ...PersonArgs,
      isPerson: true,
      size: '2xl',
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

### Avatar Alignment

<Canvas>
  <Story name="Avatar Alignment">
    <Avatar {...PersonArgs} isPerson />
    <Avatar {...PersonArgs} isPerson src="https://placekitten.com/32/32" />
    <Avatar {...OrgArgs} />
    <Avatar {...OrgArgs} src="https://placekitten.com/100/100" />
  </Story>
</Canvas>
