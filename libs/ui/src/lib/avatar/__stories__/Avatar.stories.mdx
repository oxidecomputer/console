<!-- Avatar.stories.mdx -->

import { ArgsTable, Canvas, Story, Meta } from '@storybook/addon-docs/blocks'
import { Avatar, AvatarProps, avatarSizes } from '../Avatar'
import {
  Person,
  Organization,
  stories,
  AvatarAlignment,
} from './Avatar.stories'

<Meta
  title="Components/Avatar"
  component={Avatar}
  argTypes={{
    isPerson: {
      control: {
        type: 'boolean',
      },
    },
    name: {
      control: {
        type: 'text',
      },
    },
    src: {
      control: {
        type: 'text',
      },
    },
    size: {
      control: {
        type: 'select',
        options: Object.keys(avatarSizes),
      },
    },
  }}
/>

# Avatar

Avatars are used to represent a unique entity, be it a person, a team, an org, or a project.

export const Template = (args) => <Avatar {...args} />

export const PersonArgs = {
  name: 'Cameron Howe',
  isPerson: true,
}

export const OrgArgs = {
  name: 'Colossal Cave Adventure',
}

<Canvas>
  <Story story={Person} />
  <Story story={Organization} />
</Canvas>

<ArgsTable of={Avatar} story="^" />

## Design

Avatars representing **persons** use a circular shape and avatars representing **a group or a project** use a square shape. They come in a variety of sizes. The size of an avatar varies depending upon the element it resides in as well as the size of the viewport.

It is preferable that an avatar display an image. Images are personalized and make good use of the ability of the human brain to recognize faces. Project and org avatars contain a fallback in the case an image is not provided. This fallback will display the first letter(s) of the project or org name. Personal avatars have a Gravatar fallback which contains either a configured image or a randomly generated image.

## Stories

### Organization with an image

<Canvas>
  <Story story={stories.organization_image_sm} />
  <Story story={stories.organization_image_base} />
  <Story story={stories.organization_image_lg} />
  <Story story={stories.organization_image_xl} />
  <Story story={stories.organization_image_2xl} />
</Canvas>

### Organization fallback

<Canvas>
  <Story story={stories.organization_fallback_sm} />
  <Story story={stories.organization_fallback_base} />
  <Story story={stories.organization_fallback_lg} />
  <Story story={stories.organization_fallback_xl} />
  <Story story={stories.organization_fallback_2xl} />
</Canvas>

### Person with an image

<Canvas>
  <Story story={stories.person_image_sm} />
  <Story story={stories.person_image_base} />
  <Story story={stories.person_image_lg} />
  <Story story={stories.person_image_xl} />
  <Story story={stories.person_image_2xl} />
</Canvas>

### Person fallback

<Canvas>
  <Story story={stories.person_fallback_sm} />
  <Story story={stories.person_fallback_base} />
  <Story story={stories.person_fallback_lg} />
  <Story story={stories.person_fallback_xl} />
  <Story story={stories.person_fallback_2xl} />
</Canvas>

### Avatar Alignment

<Canvas>
  <Story story={AvatarAlignment} />
</Canvas>
