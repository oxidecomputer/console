<!-- Icons.stories.mdx-->

import { Meta } from '@storybook/addon-docs/blocks'
import styled from 'styled-components'
import { Icon, icons } from './Icon'

<Meta title="Icons" component={Icon} />

# Icons

Making inline SVG icons accessible: https://www.deque.com/blog/creating-accessible-svgs/

<table>
  <tr>
    <th>Icon</th>
    <th>Name</th>
  </tr>
  {Object.keys(icons).map((key) => {
    return (
      <tr>
        <td>
          <Icon name={key} />
        </td>
        <td>
          <code>{key}</code>
        </td>
      </tr>
    )
  })}
</table>
