<!-- Text.stories.mdx-->

import { Canvas, Story, Meta } from '@storybook/addon-docs/blocks'
import styled from 'styled-components'
import { Text, sizes } from './Text'
import { colors } from '../theme'

<Meta
  title="Text"
  component={Text}
  argTypes={{
    color: {
      control: {
        type: 'select',
        options: Object.keys(colors),
      },
    },
    font: {
      control: {
        type: 'select',
        options: ['sans', 'mono'],
      },
    },
    size: {
      control: {
        type: 'select',
        options: sizes,
      },
    },
    weight: {
      control: {
        type: 'number',
      },
    },
  }}
/>

# Text

export const Template = (args) => <Text {...args} />

Text will render as a `<span>` by default.

Text can be rendered as another element using the `as` prop (built in from `styled-components`), but please be sure to use the correct semantic element to maintain an <a href="https://www.w3.org/WAI/WCAG21/Understanding/headings-and-labels.html">accessible Document Outline</a> and rely on styles for changing the appearance, see <a href="https://www.w3.org/WAI/standards-guidelines/wcag/">Web Content Accessibility Guidelines (WCAG)</a> for more detail.

<Canvas>
  <Story
    name="Default"
    args={{
      children: `Text will render as a 'span' by default.`,
      font: 'sans',
      size: 'base',
    }}
  >
    {Template.bind({})}
  </Story>
  <Story
    name="Render as a p tag"
    args={{
      as: 'p',
      children: `Text can be rendered in a <p> tag using the 'as' prop.`,
      font: 'sans',
      size: 'base',
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

## Text Sizes

<Canvas>
  <Story
    name="xxs"
    args={{
      children: `'xxs' size text`,
      size: 'xxs',
    }}
  >
    {Template.bind({})}
  </Story>
  <Story
    name="xs"
    args={{
      children: `'xs' size text`,
      size: 'xs',
    }}
  >
    {Template.bind({})}
  </Story>
  <Story
    name="sm"
    args={{
      children: `'sm' size text`,
      size: 'sm',
    }}
  >
    {Template.bind({})}
  </Story>
  <Story
    name="base"
    args={{
      children: `'base' size text`,
      size: 'base',
    }}
  >
    {Template.bind({})}
  </Story>
  <Story
    name="lg"
    args={{
      children: `'lg' size text`,
      size: 'lg',
    }}
  >
    {Template.bind({})}
  </Story>
  <Story
    name="xl"
    args={{
      children: `'xl' size text`,
      size: 'xl',
    }}
  >
    {Template.bind({})}
  </Story>
  <Story
    name="2xl"
    args={{
      children: `'2xl' size text`,
      size: '2xl',
    }}
  >
    {Template.bind({})}
  </Story>
  <Story
    name="3xl"
    args={{
      children: `'3xl' size text`,
      size: '3xl',
    }}
  >
    {Template.bind({})}
  </Story>
  <Story
    name="4xl"
    args={{
      children: `'4xl' size text`,
      size: '4xl',
    }}
  >
    {Template.bind({})}
  </Story>
  <Story
    name="5xl"
    args={{
      children: `'5xl' size text`,
      size: '5xl',
    }}
  >
    {Template.bind({})}
  </Story>
  <Story
    name="6xl"
    args={{
      children: `'6xl' size text`,
      size: '6xl',
    }}
  >
    {Template.bind({})}
  </Story>
  <Story
    name="7xl"
    args={{
      children: `'7xl' size text`,
      size: '7xl',
    }}
  >
    {Template.bind({})}
  </Story>
  <Story
    name="8xl"
    args={{
      children: `'8xl' size text`,
      size: '8xl',
    }}
  >
    {Template.bind({})}
  </Story>
  <Story
    name="9xl"
    args={{
      children: `'9xl' size text`,
      size: '9xl',
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>
