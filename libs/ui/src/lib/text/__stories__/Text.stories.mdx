<!-- Text.stories.mdx-->

import { ArgsTable, Canvas, Story, Meta } from '@storybook/addon-docs/blocks'
import styled from 'styled-components'
import { colorPalette } from '@oxide/theme'
import { Text, textSizes } from '../Text'
import { stories, textSizeStories } from './Text.stories'

<Meta
  title="Foundations/Text"
  component={Text}
  argTypes={{
    color: {
      control: {
        type: 'select',
        options: [null, ...Object.keys(colorPalette)],
      },
    },
    font: {
      control: {
        type: 'select',
        options: ['sans', 'mono'],
      },
    },
    size: {
      control: {
        type: 'select',
        options: textSizes,
      },
    },
    weight: {
      control: {
        type: 'range',
        min: 100,
        max: 900,
        step: 100,
      },
    },
    theme: { table: { disable: true } },
    ref: { table: { disable: true } },
    as: { table: { disable: true } },
    forwardedAs: { table: { disable: true } },
  }}
/>

# Text

export const Template = ({ children, ...args }) => (
  <Text {...args}>{children}</Text>
)

Text will render as a `<span>` by default.

Text can be rendered as another element using the `as` prop (built in from `styled-components`), but please be sure to use the correct semantic element to maintain an <a href="https://www.w3.org/WAI/WCAG21/Understanding/headings-and-labels.html">accessible Document Outline</a> and rely on styles for changing the appearance, see <a href="https://www.w3.org/WAI/standards-guidelines/wcag/">Web Content Accessibility Guidelines (WCAG)</a> for more detail.

<Canvas>
  <Story story={stories.default} />
  <Story story={stories.asPTag} />
</Canvas>

<ArgsTable of={Text} story="^" />

## Stories

### Left Icon

<Canvas>
  <Story story={stories.withIcon} />
</Canvas>

### Right Icon

<Canvas>
  <Story story={stories.withRightIcon} />
</Canvas>

#### Title Variant with Icon

<Canvas>
  <Story story={stories.titleWithIcon} />
</Canvas>

### Sizes

<Canvas>
  <Story story={stories.xxs} />
  <Story story={stories.xs} />
  <Story story={stories.sm} />
  <Story story={stories.base} />
  <Story story={stories.lg} />
  <Story story={stories.xl} />
  <Story story={stories.two_xl} />
  <Story story={stories.three_xl} />
  <Story story={stories.four_xl} />
  <Story story={stories.five_xl} />
  <Story story={stories.six_xl} />
  <Story story={stories.seven_xl} />
  <Story story={stories.eight_xl} />
  <Story story={stories.nine_xl} />
</Canvas>

### Variants

#### Title

<Canvas>
  <Story story={stories.titleVariant} />
</Canvas>
