import { ArgsTable, Canvas, Story, Meta } from '@storybook/addon-docs'
import { Avatar, AvatarProps, avatarSizes, avatarColors } from '../Avatar'
import { Person, AvatarAlignment } from './Avatar.stories'

<Meta
  title="Components/Avatar"
  component={Avatar}
  argTypes={{
    round: { control: { type: 'boolean' } },
    size: {
      control: {
        type: 'select',
        options: avatarSizes,
      },
    },
  }}
/>

# Avatar

Avatars are used to represent a unique entity, be it a person, a team, an org, or a project.

<Canvas>
  <Story story={Person} />
</Canvas>

<ArgsTable of={Avatar} story="^" />

## Round

<Canvas>
  <div className="flex gap-8">
    <div className="flex flex-col gap-4">
      {avatarSizes.map((size) => (
        <Avatar
          size={size}
          name="Colossal Cave Adventure"
          src="http://placekitten.com/500/500"
          round
        />
      ))}
    </div>
    {avatarColors.map((color) => (
      <div className="flex flex-col gap-4">
        {avatarSizes.map((size) => (
          <Avatar size={size} name="Cameron Howe" color={color} round />
        ))}
      </div>
    ))}
  </div>
</Canvas>

## Square

<Canvas>
  <div className="flex gap-8">
    <div className="flex flex-col gap-4">
      {avatarSizes.map((size) => (
        <Avatar
          size={size}
          name="Colossal Cave Adventure"
          src="http://placekitten.com/500/500"
        />
      ))}
    </div>
    {avatarColors.map((color) => (
      <div className="flex flex-col gap-4">
        {avatarSizes.map((size) => (
          <Avatar size={size} name="Cameron Howe" color={color} />
        ))}
      </div>
    ))}
  </div>
</Canvas>
