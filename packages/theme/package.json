{
  "name": "@oxide/theme",
  "private": true,
  "version": "0.0.0",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "dev": "tsc --watch --preserveWatchOutput",
    "storybook:dev": "yarn dev",
    "build": "tsc",
    "clean": "rm -rf dist"
  }
}