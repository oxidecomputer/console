export interface Theme {
  colors: {
    primary: string
    secondary: string
    background: string
    navBackground: string
  }

  spacing: {
    small: string
    medium: string
    large: string
  }
}
