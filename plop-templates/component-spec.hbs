import React from 'react'
import { render } from '{{testUtilsRelPath}}'

import { {{name}} } from './{{name}}'

describe('{{name}}', () => {
  it('should render successfully', () => {
    const { container } = render(<{{name}} />)
    expect(container).toBeTruthy()
  })
})
